
# AI福尔摩斯案件推理游戏产品体验优化方案

# 待确认的点（优先看这个）
直接选择案件生成agent做这个事好还是单独设置一个全局视角npc？

## npc列表
## 1. 华生npc：
一个辅助用户勘察➕审讯➕推理的、智商合格但不优秀的npc      
### 交互方式：                  
    * 用户在整个游戏过程中都可以和其进行沉浸式对话，理论上可以问华生任何问题。
    * 用户在勘察现查时：进入每一个现场时，华生一开始会根据当前场景主动给出一条勘察的提示，并提醒用户随时可以咨询下一步勘察提示，帮助用户找到线索（但华生的提示不一定全都对，主要是为了提升用户的互动感）；
    * 用户在审讯犯人时：华生会在一旁根据当前审讯内容，全程实时结合已有线索对犯人说的话进行矛盾检测和可疑点检测，给用户一些可疑信息提示，帮助发现犯人线索（华生给的提示不一定都对，需要用户自行判断）
    * 用户在推理时：结合已有的线索和相关犯人的关键发言线索，给到用户一些可能的提示，如某某用户的发言和某一条线索似乎有点关联，可以重点关注
## 2. 全局视角npc：
推理板过程中，根据用户提交的线索链和推理结论，结合本案件的所有线索信息和结局判断用户的推论是否正确
### 交互方式：
    * 用户在推理阶段，构建了一条线索链和推理结论后可以提交给其进行验证。
## 3. 场景npc
一个场景一个npc，如客厅npc、死者尸体npc、书房npc等
### 交互方式：
    * 用户在点击每一个场景探索时，弹出对应场景的页面和npc对话界面，用户通过与npc对话的形式探查场景，寻找线索


# 游戏细节优化：
## 1. 场景勘探：
### 前端展示：
    * 三列展示，中间列展示场景氛围图，左侧列展示场景信息描述（如办公室场景：办公桌、书桌、保险柜、电脑等），右侧列展示场景npc对话窗口
### 功能交互：
    * 点击一个场景后，弹出一个新的页面，用户可以在新页面的对话框中进行提问要求，如搜查一下办公桌，npc会返回办公桌上的信息给到用户，用户可以进一步搜索如看看办公桌上的信件内容，npc会返回信件内容给用户。如此反复。
    * 如果用户评估npc返回的内容是一条关键线索，用户可以选择将该条信息添加到线索列表，需要让用户输入线索名称（如信件名称，方便后续推理）
## 2. 推理板：
### 前端展示：
    ```bash
        Design a detective推理游戏“推理板块”页面，桌面端布局，左右两栏结构。

        【左侧】线索面板
        - 标题：“📋 线索列表”
        - 展示多条线索卡片，每条线索包含：图标（如信件、怀表、门锁）、线索名称、简短描述
        - 每条线索左侧有一个复选框（☑️），支持多选
        - 底部显示“已选 X 条线索”和按钮“✨ 组合推理”

        【右侧】推理记录面板
        - 标题：“📝 推理记录”
        - 按时间倒序展示推理记录卡片，每条卡片包含：
        - 推理编号（如 #4）
        - 验证结果徽章（✅ 正确 / ❌ 错误 / ⚠️ 部分正确）
        - 关联线索标签列表（chip 样式）
        - 推理结论文本
        - 时间戳
        - 支持筛选器：全部 / 正确 / 错误
        - 每条记录支持右键菜单：标记重要、删除

        【右下角全局】指认凶手按钮
        - 固定悬浮在页面右下角，圆形或胶囊形，红色/金色渐变，带阴影
        - 文字：“🔍 指认凶手”
        - 默认半透明灰色不可点（disabled），满足条件后变为高亮可点
        - 点击后弹出模态框：嫌疑人列表（带头像和姓名）、需选择1-3条推理记录作为指控依据、提交按钮

        【组合推理弹窗】
        - 点击“组合推理”后弹出居中模态框
        - 显示已选线索缩略列表
        - 多行输入框：“根据以上线索，你的推理是…”
        - 按钮：“提交推理并验证”
        - 提交后自动关闭弹窗，并在右侧推理记录面板顶部新增一条记录

        整体风格：暗色推理主题（深灰背景、金色强调色），卡片有轻微边框和发光效果，字体清晰易读。
    ```
### 功能优化：
    1、用户添加的所有线索都会关联到当前页面的线索面板，用户可以随时查看已添加的线索
    2、用户在线索面板选择一条或多条线索，点击“组合推理”按钮并提交给全局视角npc进行验证后，在右侧推理记录面板新增一条推理记录
    3、用户点击“指认凶手”按钮，弹出一个模态框，用户需要选择1-3条推理记录作为指控依据，提交后，全局视角npc会根据用户选择的推理记录和案件信息判断用户的推理是否正确，若正确，则指认凶手，若错误，则提示用户重新推理
## 3、审讯：
### 前端展示：
    * 保持当前模式不变
### 功能优化：
    * 审讯嫌疑人过程中，如果用户觉得嫌疑人的某句/某几句话有问题，用户可以将该句/该几句话选中，点击“生成线索”按钮，生成一条线索，需要让用户输入线索名称（如“嫌疑人的发言，该句/该几句话有问题”，方便后续推理）
    * 用户审问犯人时，华生npc需要在一旁给到一些提示审问话术




